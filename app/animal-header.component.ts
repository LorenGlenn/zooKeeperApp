import { Component, Input, Output, EventEmitter } from '@angular/core';
import { Animal } from './animal.model';

@Component({
  selector: 'animal-header',
  template: `
  <div class="jumbotron">
  </div>`
})

  export class AnimalHeaderComponent {

  }
